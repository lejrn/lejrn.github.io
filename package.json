{
  "name": "page",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "fetch-data": "node scripts/fetch-repos.js",
    "build": "node scripts/fetch-repos.js && vite build",
    "build:only": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
